name: Update badges

on: workflow_dispatch
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: rbkl984/README.md@main
        with:       
          CREDLY_USER: "rbk l984" # optional, but default will use the same from github
